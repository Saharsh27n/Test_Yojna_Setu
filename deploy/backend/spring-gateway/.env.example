# ── Supabase PostgreSQL ─────────────────────────────────────────────────────────
# Get these from: Supabase Dashboard → Project Settings → Database
# Use port 5432 (direct/session mode) — NOT 6543 (transaction pooler)


MONGO_URI=mongodb+srv://pranjal11246_db_user:Battosai#11246@cluster0.cs1902j.mongodb.net/yojnadb?retryWrites=true&w=majority&appName=Cluster0



# ── JWT Secret ───────────────────────────────────────────────────────────────────
# Generate a long random string: openssl rand -hex 32
JWT_SECRET=<CHANGE-ME-use-openssl-rand-hex-32>

# ── Services ─────────────────────────────────────────────────────────────────────
FASTAPI_URL=http://localhost:8001
FRONTEND_URL=http://localhost:5173
